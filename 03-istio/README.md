# Istio Mesh and Ingress Controller

[Homepage](https://istio.io/)

## Installation

Add the command line tool and use it to install the istio manifests

``` shell
istioctl manifest apply --set profile=default
```
