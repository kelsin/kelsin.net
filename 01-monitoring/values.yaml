alertmanager:
    enabled: false
prometheus:
    prometheusSpec:
        serviceMonitorNamespaceSelector:
            matchLabels:
                monitor: "true"
        podMonitorNamespaceSelector:
            matchLabels:
                monitor: "true"
    ingress:
        enabled: false
grafana:
    admin:
        existingSecret: grafana-admin
    dashboards:
        default:
            ceph-cluster:
                gnetId: 2842
                datasource: Prometheus
            ceph-osd:
                gnetId: 5336
                datasource: Prometheus
            ceph-pool:
                gnetId: 5342
                datasource: Prometheus
    ingress:
        enabled: true
        annotations:
            cert-manager.io/cluster-issuer: letsencrypt-production
            external-dns.alpha.kubernetes.io/hostname: grafana.kelsin.net
        hosts:
            - grafana.kelsin.net
        tls:
            - secretName: grafana-server-tls
              hosts:
                  - grafana.kelsin.net
